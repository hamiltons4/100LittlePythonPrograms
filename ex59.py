a = [1, 2, 3]

for item in a:
  # print("Item" + item + type(item))
    print(item)
    print(type(item))
    
    
for x in range(0, len(a)):
    print(x)
    
for i, item in enumerate(a):
    print("Item " + str(item)+ " " + "has index " + str(i))
